<h1>Connexion</h1>

<div class="form-container">
    <?php if (isset($error)): ?>
        <div class="error"><?= htmlspecialchars($error) ?></div>
    <?php endif; ?>

    <?php if (isset($_GET['signup_success'])): ?>
        <div class="alert alert-success">Inscription réussie ! Vous pouvez vous connecter.</div>
    <?php endif; ?>

    <form method="POST" class="form-account">
        <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" value="<?= htmlspecialchars($savedEmail) ?>" required>
        </div>

        <div class="form-group">
            <label for="password">Mot de passe</label>
            <input type="password" id="password" name="password" required>
        </div>

        <div class="form-group" style="display: flex; align-items: center; gap: 10px; width: 100%;">
            <input type="checkbox" id="remember" name="remember" <?= !empty($savedEmail) ? 'checked' : '' ?> style="width: auto;">
            <label for="remember" style="margin-bottom: 0; font-weight: normal;">Se souvenir de moi</label>
        </div>

        <button type="submit" class="btn btn-submit">Se connecter</button>
    </form>
</div>